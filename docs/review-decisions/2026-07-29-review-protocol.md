# Master-Visual Review Protocol

The user-facing design approval protocol is:

- show one desktop reference per master template;
- do not routinely show tablet or mobile references;
- keep tablet and mobile rendering as internal responsive QA;
- do not mark a master template approved until the user explicitly accepts the desktop design;
- after approval, proceed to the next master template without repeating already approved pages.