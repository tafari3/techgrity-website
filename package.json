{
  "name": "techgrity-website",
  "version": "1.0.0",
  "private": true,
  "description": "Production website for Techgrity Systems",
  "scripts": {
    "build": "python3 -m pip install --disable-pip-version-check --no-deps -q -r scripts/techgrity-corporate-render-requirements.txt && python3 scripts/sync-techgrity-corporate-assets.py && node scripts/build.js",
    "validate": "node scripts/validate.js && node scripts/validate-audit-remediation.js",
    "test": "npm run build && npm run validate && npm run test:forms && npm run test:server",
    "serve": "node server.js",
    "test:forms": "node scripts/test-forms.js",
    "start": "node server.js",
    "test:server": "node scripts/test-server.js"
  },
  "engines": {
    "node": "24.x"
  }
}
